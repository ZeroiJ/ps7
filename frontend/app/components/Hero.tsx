"use client";

// Hero component removed per user request — page starts directly with work content.
interface HeroProps {
  onUploadClick: () => void;
  onSampleClick: () => void;
}

export default function Hero(_props: HeroProps) {
  return null;
}
